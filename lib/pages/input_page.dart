import 'package:flutter/material.dart';

class InputPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("약물 입력")),
      body: Center(child: Text("이곳은 input_page입니다!")),
    );
  }
}
